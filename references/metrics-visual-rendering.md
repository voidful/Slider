# Metrics Visual Rendering

Allow a result-heavy slide to show both metric cards and a bound visual.

When a `metrics` slide includes a bound visual asset:
- keep the metric cards readable
- place the visual beside or below the cards
- keep one dominant claim
- use the visual only when it supports the numbers

If no reliable visual is bound, keep the standard metrics-card layout and preserve the fallback reason.
